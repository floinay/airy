import {SimpleAutocompletePipe} from './simpleAutocomplete.pipe';

describe('AutocompletePipe', () => {
  it('create an instance', () => {
    const pipe = new SimpleAutocompletePipe();
    expect(pipe).toBeTruthy();
  });
});
