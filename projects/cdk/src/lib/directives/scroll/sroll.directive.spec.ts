import {ScrollDirective} from './scroll.directive';

describe('SrollDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollDirective();
    expect(directive).toBeTruthy();
  });
});
