import {ReplaceVarsPipe} from './replace-vars.pipe';

describe('ReplaceVarsPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceVarsPipe();
    expect(pipe).toBeTruthy();
  });
});
