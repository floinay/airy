import {ActionsContainerDirective} from './actions-container.directive';

describe('ActionsContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new ActionsContainerDirective();
    expect(directive).toBeTruthy();
  });
});
