import {StretchDirective} from './stretch.directive';

describe('StretchDirective', () => {
  it('should create an instance', () => {
    const directive = new StretchDirective();
    expect(directive).toBeTruthy();
  });
});
