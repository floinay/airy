import {ImpositionContainerDirective} from './imposition-container.directive';

describe('ImpositionContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new ImpositionContainerDirective();
    expect(directive).toBeTruthy();
  });
});
