import {ImpositionDirective} from './imposition.directive';

describe('ImpositionDirective', () => {
  it('should create an instance', () => {
    const directive = new ImpositionDirective();
    expect(directive).toBeTruthy();
  });
});
