import {HeaderContainerDirective} from './header-container.directive';

describe('HeaderTitleContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderContainerDirective();
    expect(directive).toBeTruthy();
  });
});
