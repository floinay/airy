import { FirstOnLineDirective } from './first-on-line.directive';

describe('FirstOnLineDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstOnLineDirective();
    expect(directive).toBeTruthy();
  });
});
