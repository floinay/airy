import { LastOnLineDirective } from './last-on-line.directive';

describe('EndOnLineDirective', () => {
  it('should create an instance', () => {
    const directive = new LastOnLineDirective();
    expect(directive).toBeTruthy();
  });
});
